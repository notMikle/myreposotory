import styled from 'styled-components';
import {font} from '@/styles/common';

export const SectionTitle = styled.h2`
  ${font({Fmax:48, Fmin:20, weight:600})};
  margin-bottom: 30px;
  text-align: center;
`
